const axios = require('axios');const config = require('../config');const getUserProfile = async (accessToken) => {  try {    const response = await axios.get('https://api.github.com/user', {      headers: {        Authorization: `token ${accessToken}`      }    });    return response.data;  } catch (error) {    console.error('Error fetching GitHub profile:', error.response ? error.response.data : error.message);    throw new Error('Failed to fetch GitHub profile.');  }};const createGist = async (accessToken, filename, content, description = 'Movie Recommendation List') => {  try {    const response = await axios.post('https://api.github.com/gists', {      description: description,      public: true,      files: {        [filename]: {          content: content        }      }    }, {      headers: {        Authorization: `token ${accessToken}`,        'Content-Type': 'application/json'      }    });    return response.data;  } catch (error) {    console.error('Error creating GitHub Gist:', error.response ? error.response.data : error.message);    throw new Error('Failed to create GitHub Gist.');  }};module.exports = {  getUserProfile,  createGist};