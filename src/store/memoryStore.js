let users = [];let projects = [];module.exports = {  getUsers: () => users,  addUser: (user) => { users.push(user); return user; },  findUser: (predicate) => users.find(predicate),  getProjects: () => projects,  addProject: (project) => { projects.push(project); return project; },  findProject: (predicate) => projects.find(predicate),  findProjectIndex: (predicate) => projects.findIndex(predicate),  updateProject: (index, newProjectData) => {    if (index > -1 && index < projects.length) {      projects[index] = { ...projects[index], ...newProjectData };      return projects[index];    }    return null;  },  deleteProject: (predicate) => {    const initialLength = projects.length;    projects = projects.filter(predicate);    return projects.length < initialLength;  }};